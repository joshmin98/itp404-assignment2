This project was configured with Babel/Webpack.

To build this project run the following commands:
1. npm install
2. npm run build

The compiled files (main.js) will be located in the '/dist' directory. 
